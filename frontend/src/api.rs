pub async fn leaderboard() -> Vec<(String, i32)> {
    // Example: connect to backend, return fake data
    vec![
        ("Alice".into(), 500),
        ("Ravi".into(), 470),
        ("Nina".into(), 450)
    ]
}
